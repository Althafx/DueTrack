{
  "name": "payment-collection-system",
  "version": "1.0.0",
  "private": true,
  "description": "Payment Collection Management System — dealer/employee collection tracking with real-time balance updates",
  "workspaces": [
    "client",
    "api",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n API,CLIENT -c blue,green \"npm run dev --workspace=api\" \"npm run dev --workspace=client\"",
    "dev:api": "npm run dev --workspace=api",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client && npm run build --workspace=api",
    "build:client": "npm run build --workspace=client",
    "build:api": "npm run build --workspace=api",
    "seed": "npm run seed --workspace=api",
    "typecheck": "npm run typecheck --workspace=client && npm run typecheck --workspace=api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
